# 🚀 Borderless Components MCP 服务器

> **AI Code智能体与 UI 组件生态系统之间的通用桥梁**

[English](README.md) | [中文](README.zh-CN.md)

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![TypeScript](https://img.shields.io/badge/TypeScript-007ACC?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

## ✨ 为什么选择 Borderless Components？

厌倦了 AI 助手生成通用 HTML 或花费时间解释需要安装哪些 UI 组件？**Borderless Components** 通过提供集中式、智能的 UI 组件注册表，彻底改变了 AI 驱动的 Web 开发，让 AI 助手能够即时发现和使用组件。

当您要求 AI "创建一个带有图表和表单的仪表板"时，它现在可以准确知道从您喜欢的 UI 库中使用哪些组件——无论是 Ant Design、Material-UI、shadcn/ui 还是任何其他框架。

## 🎯 我们解决的问题

**使用 Borderless Components 之前：**
```
用户："创建一个带有数据表格和表单的管理仪表板"
AI: *生成通用 HTML 和 CSS*
用户："你能使用 Material-UI 组件吗？"
AI: *需要手动解释可用组件*
用户："现在切换到 Ant Design"
AI: *需要再次切换上下文并解释组件*
```

**使用 Borderless Components 之后：**
```
用户："使用 Material-UI 组件创建一个管理仪表板"
AI: *即时发现并使用完美的 Material-UI 组件*
用户："太好了！现在用 shadcn/ui 重新创建这个"
AI: *无缝切换到最佳的 shadcn/ui 组件*
```

## 🌟 核心功能

### 🔍 **智能组件发现**
- **智能搜索**：跨组件名称、描述和元数据的模糊匹配
- **跨框架支持**：支持 React、Vue、Angular 和原生 JS 组件
- **多注册表**：同时搜索多个 UI 库
- **使用示例**：每个组件都内置代码示例

### 🛠 **开发者优先设计**
- **MCP 协议**：原生模型上下文协议集成，实现无缝 AI 通信
- **RESTful API**：完整的 HTTP API，适用于传统应用
- **PostgreSQL**：使用 TypeORM 的可靠数据库，确保数据存储稳定
- **TypeScript**：完整的类型安全性和出色的开发体验

### 📦 **可扩展的注册表系统**
- **多框架支持**：React、Vue、Angular、Svelte 等
- **流行库**：预配置支持 Ant Design、Material-UI、shadcn/ui、Element Plus 等
- **自定义组件**：轻松添加您自己的组件库
- **版本管理**：跟踪组件版本和弃用状态

### 🚀 **生产就绪**
- **性能优化**：高效查询，支持分页和缓存
- **可扩展架构**：专为高流量应用设计
- **健康监控**：内置健康检查和监控端点
- **易于部署**：支持 Docker，提供清晰的设置说明

## 🏗 架构概览

```
┌─────────────────┐    MCP 协议      ┌─────────────────┐
│   AI 助手       │ ◄─────────────► │  Borderless     │
│  (Claude/GPT-4) │                 │  Components     │
│                 │                 │    服务器       │
└─────────────────┘                 │                 │
         │                           │                 │
         │ HTTP API                  │                 │
         │                           │                 │
┌─────────────────┐                 │                 │
│   Web/App UI    │ ◄──────────────►│                 │
│    仪表板        │    RESTful API  │                 │
└─────────────────┘                 └─────────────────┘
                                            │
                                            ▼
                                  ┌─────────────────┐
                                  │   PostgreSQL    │
                                  │     数据库       │
                                  │                 │
                                  │ • 注册表         │
                                  │ • 组件           │
                                  │ • 示例          │
                                  └─────────────────┘
```

## 🚀 快速开始

### 安装

```bash
npm install borderless-components-mcp
```

### 基本设置

1. **克隆并设置**
```bash
git clone https://github.com/your-org/borderless-components-mcp.git
cd borderless-components-mcp
npm install
```

2. **配置数据库**
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑数据库配置
# 默认: postgresql://localhost:5432/borderless_components
```

3. **初始化数据库**
```bash
npm run build
npm run seed:api  # 填充示例注册表和组件
```

4. **启动服务器**
```bash
npm run dev  # 开发模式
# 或
npm start    # 生产模式
```

5. 添加到你的Code Agent

完成！您的服务器现在运行在：
- **HTTP API**: `http://localhost:3000`
- **MCP 端点**: `http://localhost:3000/mcp`

## 🔌 集成示例

### AI 助手集成 (MCP)

```javascript
// 配置您的 AI 助手以使用 Borderless Components
const mcpConfig = {
  name: "borderless-components",
  endpoint: "http://localhost:3000/mcp",
  tools: [
    "get_available_registries",
    "get_available_ui_components_from_registries",
    "search_items_in_registries",
    "get_item_examples_from_registries"
  ]
};
```

### Web 应用集成

```javascript
// 在所有注册表中搜索组件
const response = await fetch('http://localhost:3000/api/components?query=button&limit=10');
const components = await response.json();

// 在特定注册表中搜索
const antdButtons = await fetch('http://localhost:3000/api/components?registrySlug=antd&query=button');
const shadcnButtons = await fetch('http://localhost:3000/api/components?registrySlug=shadcn&query=button');
```

## 📚 可用工具

### 🔎 **发现工具**

- **`get_available_registries`** - 列出所有可用的 UI 库
- **`get_available_ui_components_from_registries`** - 从特定注册表获取组件
- **`search_items_in_registries`** - 跨组件和注册表的智能搜索
- **`get_item_examples_from_registries`** - 获取组件的使用示例

### 🌐 **REST API 端点**

- **GET** `/api/registries` - 浏览所有注册表
- **GET** `/api/components` - 搜索和过滤组件
- **GET** `/api/components/:id/examples` - 获取组件示例
- **POST** `/api/registries` - 添加新注册表
- **POST** `/api/components` - 添加新组件

## 🎨 使用场景

### 🤖 AI 驱动的开发

**快速原型设计**：AI 助手可以即时发现并使用合适的组件，将开发时间从数小时缩短到数分钟。

**框架切换**：在保持功能的同时轻松切换 UI 框架——非常适合设计系统探索。

**代码生成**：生成生产就绪的代码，包括正确的组件使用、依赖项和安装命令。

### 👥 团队协作

**组件治理**：集中式注册表确保团队在项目中使用经过批准、一致的组件。

**知识共享**：内置示例和文档加速入职和知识传递。

**版本管理**：跟踪组件版本、弃用和迁移路径。

### 🏢 企业集成

**设计系统**：与内部组件库和设计系统集成，实现最大一致性。

**多项目管理**：从单一界面管理多个项目和团队的组件使用。

**分析与洞察**：跟踪组件使用模式，为设计系统改进和培训需求提供信息。

## 🔧 配置

### 环境变量

```bash
# 服务器配置
HTTP_PORT=3000              # HTTP API 端口
MCP_PORT=3001              # MCP 服务器端口（可选）

# 数据库
DATABASE_URL="postgresql://user:password@localhost:5432/borderless_components"

# 开发
NODE_ENV=development
```

### 添加自定义注册表

```javascript
// 添加您的自定义组件库
const customRegistry = {
  name: "我的设计系统",
  slug: "my-design-system",
  framework: "react",
  npmPackage: "@mycompany/design-system",
  installCommand: "npm install @mycompany/design-system",
  description: "我们公司的设计系统组件"
};

// POST 到 /api/registries
await fetch('http://localhost:3000/api/registries', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(customRegistry)
});
```

## 🛣 路线图

### 🎯 **即将推出的功能**

- [ ] **可视化组件浏览器** - 基于 Web 的组件浏览
- [ ] **组件依赖分析** - 自动依赖解析
- [ ] **使用分析仪表板** - 跟踪组件使用模式
- [ ] **GitHub 集成** - 与组件仓库自动同步
- [ ] **组件测试套件** - 自动化组件验证
- [ ] **主题支持** - 每个组件的多个主题变体
- [ ] **性能指标** - 组件包大小和性能数据

### 🌟 **未来愿景**

- **AI 驱动的推荐**：基于项目上下文的智能组件建议
- **跨框架转换**：在 React、Vue 和 Angular 之间转换组件
- **社区注册表**：开源库的公共组件注册表
- **设计系统生成器**：从组件使用自动生成设计系统

## 🤝 贡献

我们正在构建 AI 驱动开发的未来！我们欢迎各种形式的贡献：

- 🐛 **错误报告** - 帮助我们修复问题
- 💡 **功能请求** - 建议新功能
- 📖 **文档** - 改进指南和示例
- 🔧 **代码贡献** - 提交 Pull Request
- 🎨 **UI/UX** - 设计更好的界面
- 🧪 **测试** - 提高测试覆盖率

### 开发设置

```bash
# Fork 并克隆
git clone https://github.com/your-username/borderless-components-mcp.git
cd borderless-components-mcp

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 运行测试
npm test

# 构建生产版本
npm run build
```

### 贡献指南

1. **代码风格**：遵循 TypeScript 和 Prettier 配置
2. **测试**：为新功能和错误修复添加测试
3. **文档**：更新 README 和 API 文档
4. **提交**：使用约定式提交消息
5. **PR**：包含清晰的描述和测试说明

## 📄 许可证

本项目采用 MIT 许可证 - 详情请参阅 [LICENSE](LICENSE) 文件。

## 🙏 致谢

- **Model Context Protocol (MCP)** - 实现无缝 AI 集成
- **TypeORM** - 优秀的 TypeScript 数据库 ORM
- **Express.js** - 我们 API 的强大 Web 框架
- **PostgreSQL** - 强大的关系数据库
- **所有贡献者** - 构建 AI 驱动开发未来的优秀开发者

## 📞 联系我们

- **GitHub Issues**：[报告错误和请求功能](https://github.com/your-org/borderless-components-mcp/issues)
- **讨论**：[加入我们的社区讨论](https://github.com/your-org/borderless-components-mcp/discussions)
- **Email**: [Contact our team](jamesklh5666@gmail.com)
- **wechat group QRCode**: ![wechat QR Code](./_img/wechatgroupQR.png "QR Code")

---

<div align="center">

**⭐ 如果 Borderless Components 帮助您更快地使用 AI 构建，请给这个仓库点个星！**

**🚀 准备好彻底改变您的 AI 驱动开发了吗？**
[立即开始](#快速开始) • [查看文档](./docs) • [加入社区](https://github.com/your-org/borderless-components-mcp/discussions)

由 Borderless 团队的 LHKong7 用 ❤️ 制作

</div>

